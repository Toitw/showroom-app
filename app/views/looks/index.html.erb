<h1>Look#index</h1>

<%= turbo_frame_tag "new look" do %>
  <%= link_to 'New look', new_look_path(@look) %>
<% end %>

<div id="looks">
  <% @looks.each do |look| %>
    <%= render 'look', look: look %>
    <%= link_to 'Edit look', edit_look_path(look) %>
  <% end %>
</div>